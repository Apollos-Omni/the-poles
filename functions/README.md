# Legacy Base44 backend functions removed

The app now routes frontend function calls to the custom Node/Supabase backend in `backend/`.

The previous Base44/Deno functions were archived in `legacy_base44_functions/` for reference only. Do not deploy them as active backend functions unless you intentionally roll back.
